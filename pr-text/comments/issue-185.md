Covered by #145. It replaces all four `len(matching_result.getAggregatedFunctionMatches(...))` calls in `data.py`, the `unfiltered=True` twins included, with `count_aggregated_function_matches`. That helper counts the distinct function ids instead of building the aggregation; with `unfiltered=True` it counts the whole report. The counts equal the old ones, which `tests/testResultPages.py` in #145 asserts. #145 predates this issue, so it doesn't link here.
